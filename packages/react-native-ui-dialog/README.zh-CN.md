# @ruban-labs/react-native-ui-dialog

基于 Ruban Theme 与 Overlay peer 的裸 React Native 组合式对话框。

[English](./README.md)

通过 `Dialog.Root`、`Dialog.Trigger`、`Dialog.Content` 与 `Dialog.Close` 组合受控或非受控
对话框，并在应用根部放置一个 `OverlayProvider`。

## 许可

MIT
